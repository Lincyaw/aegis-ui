name: aegis-trajectories

services:
  clickhouse:
    image: clickhouse/clickhouse-server:24.10
    container_name: aegis-trajectories-clickhouse
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    environment:
      CLICKHOUSE_DB: otel
      CLICKHOUSE_USER: default
      CLICKHOUSE_PASSWORD: ""
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: "1"
    ports:
      - "127.0.0.1:18123:8123"   # HTTP
      - "127.0.0.1:19000:9000"   # native TCP
    volumes:
      - ch-data:/var/lib/clickhouse
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8123/ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  otelcol:
    image: otel/opentelemetry-collector-contrib:0.115.1
    container_name: aegis-trajectories-otelcol
    command: ["--config=/etc/otelcol/config.yaml"]
    volumes:
      - ./otelcol-config.yaml:/etc/otelcol/config.yaml:ro
    ports:
      # Standard OTLP ports — AgentM picks these up without env override.
      - "127.0.0.1:4317:4317"   # OTLP gRPC
      - "127.0.0.1:4318:4318"   # OTLP HTTP
    depends_on:
      clickhouse:
        condition: service_healthy

volumes:
  ch-data:
